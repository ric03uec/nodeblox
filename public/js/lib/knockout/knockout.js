//initialize knockoutjs here with a global name knockout.
//this will ensure that if a new version has to be replaced later
//on, then only the reference here will be changed
define(['lib/knockout/knockout-2.1.0'], function(KO){
  return KO;
});
